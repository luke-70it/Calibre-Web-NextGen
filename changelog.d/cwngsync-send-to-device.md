### Added

- **Books can be queued to a specific KOReader device from their detail page.**
  The reader collects each compatible format on its next sync, retries interrupted
  transfers without creating duplicate files, and skips books already observed in
  that device's library.
